pipeline {
    agent any
    stages {
        stage('Build README') {
            steps {
                echo 'Building the README...'
                sh 'cat README.md'
            }
        }
    }
}

