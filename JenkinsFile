pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Starting the build process...'
                sh 'echo "Build completed successfully." > build.log'
                sh 'ls -l'
            }
        }
    }
}
